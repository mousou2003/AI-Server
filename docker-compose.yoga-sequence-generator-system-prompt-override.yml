# Generic Docker Compose Override Template
# This file serves as a template for creating project-specific overrides
# The OllamaCustomModel class will process this template and create
# project-specific override files automatically

services:
  ollama:
    container_name: ollama-yoga-sequence-generator-system-prompt
    hostname: ollama-yoga-sequence-generator-system-prompt
    environment:
      - OLLAMA_KEEP_ALIVE=24h
      - OLLAMA_HOST=0.0.0.0
      # Add project-specific environment variables here
    volumes:
      - ./.ollama/yoga-sequence-generator-system-prompt:/root/.ollama
    networks:
      - ai_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  open-webui:
    container_name: open-webui-yoga-sequence-generator-system-prompt
    hostname: open-webui-yoga-sequence-generator-system-prompt
    environment:
      - WEBUI_NAME=Yoga Sequence Generator System Prompt
      - OLLAMA_BASE_URL=http://ollama-yoga-sequence-generator-system-prompt:11434
      - WEBUI_AUTH=False
      # Add project-specific WebUI settings here
    volumes:
      - ./.webui/yoga-sequence-generator-system-prompt/data:/app/backend/data
      - ./.webui/yoga-sequence-generator-system-prompt/workspace:/app/backend/workspace
    networks:
      - ai_network
    depends_on:
      ollama:
        condition: service_healthy

volumes:
  yoga-sequence-generator-system-prompt-data:
    driver: local
  yoga-sequence-generator-system-prompt-memory:
    driver: local  
  yoga-sequence-generator-system-prompt-workspace:
    driver: local

networks:
  ai_network:
    external: true